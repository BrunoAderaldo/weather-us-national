import { styled } from "../../../stitches.config";

const Card = styled("div", {
  backgroundColor: "#fff",
  borderRadius: "$borderRadius",
  padding: "$5",
  boxShadow: "$default",
  marginBottom: "$6",
});

export default Card;
